#2. def => used to define a function or a method in Python
def welcome(name): #'welcome' here is a function, it is defined using the def statement
    print(f"{name}, Welcome to the world")
welcome ("veni")

#✨ more about Symbols ✨
# (# hash) => comment, source encoding declaration, in string formatting, regular expressions
# (() parentheses) => call, class definition, fuction definition, generator expression, in regular expressions, tuple display
# "" => string literal
# {} (Curly Braces) => Used to define a dictionary in Python.
#                      A dictionary is an unordered collection of key-value pairs, where each key must be unique.